interface:
  display_name: "Project Workflow"
  short_description: "通用项目工作流编排、验证同步与流程沉淀模板"
  default_prompt: "Use $project-workflow to scan the repository, restore project state, resolve authority, route the task, and enforce validation plus docs sync before delivery."
