# 项目记忆 · Project Memory

> 记忆系统：回答“系统为什么这样设计、有哪些已知知识”。只记录已确认事实、明确假设和待确认项，不把模板占位当成事实。

---

## 架构与系统地图

- 当前已确认：项目已接入 Portable Agent Workflow 文档与四态骨架。
- 待确认：项目模块边界、关键依赖、运行入口、数据持久化方式和外部服务。

## 关键决策

### D-001 · 接入 Portable Agent Workflow

- **决定**：使用 `AGENTS.md`、`CLAUDE.md`、`docs/` 和 `state/` 作为多 agent 协作入口。
- **为什么**：让后续会话能先恢复状态，再规划、实现、验证、自审和提交。
- **影响**：重要变更后需要同步日志、进度、需求或记忆；不能只把结论留在对话里。
- **时间**：待确认初始化日期。

## 已知坑

- `gotcha` 初始化模板只是结构起点；首次开发前必须用项目事实替换待确认项。

## 外部资源

- `reference` 待确认：项目资料、设计稿、旧仓库、竞品、部署环境或工单链接。
